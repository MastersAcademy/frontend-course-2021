import { SignpaintDirective } from './signpaint.directive';

describe('SignpaintDirective', () => {
    it('should create an instance', () => {
        const directive = new SignpaintDirective();
        expect(directive).toBeTruthy();
    });
});
