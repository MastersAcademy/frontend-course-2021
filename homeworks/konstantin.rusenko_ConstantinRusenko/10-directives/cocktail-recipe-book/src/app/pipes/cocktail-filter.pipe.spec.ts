import { CocktailFilterPipe } from './cocktail-filter.pipe';

describe('CocktailFilterPipe', () => {
    it('create an instance', () => {
        const pipe = new CocktailFilterPipe();
        expect(pipe).toBeTruthy();
    });
});
