import { GetDurationPipe } from './get-duration.pipe';

describe('GetDurationPipe', () => {
    it('create an instance', () => {
        const pipe = new GetDurationPipe();
        expect(pipe).toBeTruthy();
    });
});
